#ifndef PQTLS_HANDSHAKE_H
#define PQTLS_HANDSHAKE_H

#include "pqtls.h"

/**
 * @file pqtls_handshake.h
 * @brief PQTLS 握手模块（实现 CLIENT_HELLO / SERVER_HELLO / SM9_CERT_VERIFY / CLIENT_KEM / FINISHED）。
 *
 * 说明：
 * - 对外握手接口已在 src/pqtls.h 中声明：
 *   - pqtls_client_handshake()
 *   - pqtls_client_handshake_with_config()
 *   - pqtls_server_handshake()
 *   - pqtls_server_handshake_with_config()
 * - 本头文件主要用于说明该模块的职责，避免与 record 层耦合。
 */

#endif /* PQTLS_HANDSHAKE_H */

